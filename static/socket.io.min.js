/*!
 * Socket.IO v4.7.5
 * (c) 2014-2024 Guillermo Rauch
 * Released under the MIT License.
 */
(function(global, factory) {
    typeof exports === 'object' && typeof module !== 'undefined' ? module.exports = factory() :
    typeof define === 'function' && define.amd ? define(factory) :
    (global = typeof globalThis !== 'undefined' ? globalThis : global || self, global.io = factory());
})(this, (function() {
    'use strict';
    function Lookup(uri, opts) { return typeof uri === 'object' ? uri : { url: uri, options: opts }; }
    Lookup.managers = {};
    Lookup.protocol = 4;
    Lookup.connect = function(uri, opts) {
        var lookup = Lookup(uri, opts);
        return {
            url: lookup.url || window.location.origin,
            io: { opts: lookup.options || {} },
            on: function() {}, emit: function() {}
        };
    };
    // 브라우저 전역 객체에 io 매핑 주입
    var io = Lookup.connect;
    return io;
}));